/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/Mera-Dukan-Mera-Godam',
  images: {
    unoptimized: true,
  },
  // experimental ke andar images mat rakhiye
};

export default nextConfig;